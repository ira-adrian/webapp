{{ form_start(form, {'action': path('expediente_new', { 'id': expediente.agente.id,'tipo':'int' }), 'method': 'POST'})}}
    {{ form_widget(form) }}
    {{ form_widget(form) }}
       <div class="text-right">
            <input class="btn btn-sm btn-default"  type="submit" value="Aceptar" />
        </div>
{{ form_end(form) }}
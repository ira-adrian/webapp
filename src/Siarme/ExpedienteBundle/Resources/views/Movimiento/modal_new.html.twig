{# Siarme\ExpedienteBundle\Resources\views\Expediente\modal_new.html.twig#}

<div class="modal fade" id="modalMovimientoNew" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <span class="glyphicon glyphicon-folder-open text-warning"></span> Expediente Iniciado N° {{ expediente }}
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
        <div class="modal-body">
            {{ form_start(form,  {'action': path('modal_movimiento_new', {'id':expediente.id}), 'method': 'POST'}) }}
                {{ form_widget(form) }}
                        <div class="text-right">
                  
                             <input class="btn btn-danger" type="submit" value="CONFIRMAR" />
                        </div>
            {{ form_end(form) }}
        </div>
        <div class="modal-footer">
        </div>
    </div>
  </div>
</div>
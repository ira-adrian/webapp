
    {% for tramite in documento.tramite.oferta  %}
             {% set oferente = tramite.oferente %}
            * Para la firma {{ tramite.proveedor  | default(oferente)}}  - CUIT: {{ tramite.cuit  | default("") | mostrar_como_cuil }}, los ítems N° <strong>
                    {% for itemOferta in tramite.itemOferta %}
                        {{itemOferta.numero}},
                    {% else %}  
                      -     
                    {% endfor %}
                </strong>
                 por la suma de {{ numero_a_letras(tramite.montoOfertado,2, '') }} (${{ tramite.montoOfertado | number_format(2, ',', '.')  }})
      <br><br>
      {% endfor %}
